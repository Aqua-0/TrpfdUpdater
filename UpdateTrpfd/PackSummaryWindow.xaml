<Window x:Class="TrpfdManager.PackSummaryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Mods Summary" Height="600" Width="920"
        Background="{DynamicResource WindowBg}" Foreground="{DynamicResource TextFg}">
    <Grid Margin="12">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="4*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DataGrid x:Name="Grid"
              ItemsSource="{Binding}"
              AutoGenerateColumns="False"
              CanUserAddRows="False"
              IsReadOnly="True"
              HeadersVisibility="Column"
              Margin="0,0,8,0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Pack" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Files" Binding="{Binding FileCount}" Width="80"/>
                <DataGridTextColumn Header="Wins" Binding="{Binding Wins}" Width="80"/>
                <DataGridTextColumn Header="Loses" Binding="{Binding Loses}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>

        <GroupBox Header="Files (selected pack)" Grid.Column="1" Margin="8,0,0,0">
            <ListBox ItemsSource="{Binding ElementName=Grid, Path=SelectedItem.Files}"
               FontFamily="Consolas" FontSize="12"/>
        </GroupBox>

        <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="Close" Padding="12,6" Click="Close_Click"/>
        </StackPanel>
    </Grid>
</Window>