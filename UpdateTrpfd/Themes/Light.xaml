<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- Palette -->
    <SolidColorBrush x:Key="WindowBg" Color="#F6F7FB"/>
    <SolidColorBrush x:Key="PanelBg"  Color="#FFFFFF"/>
    <SolidColorBrush x:Key="InputBg"  Color="#FFFFFF"/>
    <SolidColorBrush x:Key="BorderBr" Color="#D5DAE3"/>
    <SolidColorBrush x:Key="TextFg"   Color="#1D2430"/>
    <SolidColorBrush x:Key="MutedFg"  Color="#5C6574"/>
    <SolidColorBrush x:Key="AccentBr" Color="#3366FF"/>
    <SolidColorBrush x:Key="LogBg"    Color="#F0F2F7"/>
    <SolidColorBrush x:Key="LogFg"    Color="#1D2430"/>
    <SolidColorBrush x:Key="FgText"  Color="#E7EBF2"/>
    <SolidColorBrush x:Key="FgMuted" Color="#AAB1BF"/>
    <SolidColorBrush x:Key="BgPanel" Color="#262933"/>
    <SolidColorBrush x:Key="BgWindow" Color="#1F2026"/>
    <SolidColorBrush x:Key="BorderBrush" Color="#D5DAE3"/>
    <SolidColorBrush x:Key="TabHeaderBg"          Color="#ECEFF6"/>
    <SolidColorBrush x:Key="TabHeaderBgSelected"  Color="#DEE5F2"/>
    <SolidColorBrush x:Key="TabHeaderBorder"      Color="#C8D0E0"/>

    <!-- Styles identical to Dark but with light resources -->
    <Style TargetType="Window">
        <Setter Property="Background" Value="{DynamicResource WindowBg}"/>
        <Setter Property="Foreground" Value="{DynamicResource TextFg}"/>
    </Style>

    <Style TargetType="TextBlock">
        <Setter Property="Foreground" Value="{DynamicResource TextFg}"/>
    </Style>

    <Style TargetType="TextBox">
        <Setter Property="Foreground" Value="{DynamicResource TextFg}"/>
        <Setter Property="Background" Value="{DynamicResource InputBg}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderBr}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="6,4"/>
    </Style>

    <Style TargetType="Button">
        <Setter Property="Foreground" Value="{DynamicResource TextFg}"/>
        <Setter Property="Background" Value="{DynamicResource InputBg}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderBr}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="10,6"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border CornerRadius="8"
                  Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="6,2"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="BorderBrush" Value="{DynamicResource AccentBr}"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter Property="Background" Value="#EEF1F8"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.55"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="CheckBox">
        <Setter Property="Foreground" Value="{DynamicResource TextFg}"/>
    </Style>
    <Style TargetType="ListBox">
        <Setter Property="Background" Value="{DynamicResource PanelBg}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderBr}"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>
    <Style TargetType="TabControl">
        <Setter Property="Background" Value="{DynamicResource PanelBg}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource BorderBr}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8"/>
    </Style>
</ResourceDictionary>
